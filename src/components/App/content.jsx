import React from 'react';

import { List } from '../Card';
import { Compose } from '../Compose';

const Content = () => (
  <div className="container">
    <Compose />
    <List />
  </div>
);

export { Content };
